# Vitest reads this with higher precedence than .env, so a developer's local
# cloud-mode .env never leaks into tests. Tests assert the default-local
# behavior; CI never has a .env at all.
VITE_API_BASE_URL=
VITE_API_MODE=
VITE_REQUIRE_AUTH=
